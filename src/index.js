import React from 'react'
import ReactDOM from 'react-dom'
import App from './containers/App'
import TestHooks from './components/TestHooks'

// ReactDOM.render(<App />, document.getElementById('app')) // render('Que vamos a empujar','Dónde lo vamos a empujar')
ReactDOM.render(<TestHooks />, document.getElementById('app')) // render('Que vamos a empujar','Dónde lo vamos a empujar')
